library(ggplot2)
ggplot(data = X, aes(x = Depth, y  = Total, fill = Species)) +
  geom_area(position = "stack") +
  coord_flip() +
  scale_x_reverse()

# area plot

#this should do it really - just need to incorporate into function


https://rpubs.com/thoughtfulbloke/kitegraph

